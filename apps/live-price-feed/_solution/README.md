# Interviewer-only material — DO NOT UPLOAD

This `_solution/` folder is **not** part of the candidate package. When you upload
this exercise to the code-interview service, upload the app folder **without** this
directory.

- `App.solution.tsx` — the clean reference implementation. Drop it in over
  `src/App.tsx` (and rename its export to `App`) if you want to run the fixed
  version locally.
- `ANSWER_KEY.private.md` — every planted bug, its fix, repro steps, and the scoring
  rubric.

The candidate sees only: `src/`, `index.html`, the config files, and the top-level
`README.md`.
